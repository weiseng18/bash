alias cl="clear"
alias rl="source ~/.bashrc"

alias v="vim"
alias n="nvim"

# name terminal tab
# tab $1
function tab() {
  if [[ -z "$ORIG" ]]; then
    ORIG=$PS1
  fi
  TITLE="\[\e]2;$*\a\]"
  PS1=${ORIG}${TITLE}
}
